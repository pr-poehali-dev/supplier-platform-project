# 💳 Система оплаты и бронирования

## Как это работает?

### 1. Настройка платежных реквизитов
- Зайдите в **Админ-панель** (кнопка "Админка" в навигации)
- В разделе **"Платежные реквизиты"** для каждого объекта добавьте:
  - Платежную систему (СБП, Карта, ЮMoney и т.д.)
  - Имя получателя (опционально)
  - Ссылку для оплаты с плейсхолдерами:
    - `{amount}` — автоматически подставится сумма
    - `{description}` — описание брони

**Пример ссылки СБП:**
```
https://qr.nspk.ru/AS1A006ABCDEFG12345?type=01&bank=100000000111&sum={amount}&cur=RUB&crc=1234
```

**Пример ЮMoney:**
```
https://yoomoney.ru/quickpay/confirm?receiver=410012345678901&sum={amount}&label={description}
```

### 2. Как клиент бронирует через Telegram-бота

1. **Клиент заходит в бота** по вашей уникальной ссылке (из админки)
2. **Бот помогает выбрать даты** и объект через AI-диалог
3. **Бот генерирует персональную платежную ссылку** с правильной суммой для выбранного дома
4. **Клиент переходит по ссылке** и оплачивает
5. **Клиент отправляет скриншот чека** в бот

### 3. Автоматическая проверка AI

- GPT-4 Vision анализирует скриншот:
  - ✅ Проверяет, что это чек оплаты
  - ✅ Сверяет сумму (±50 руб допустимо)
  - ✅ Проверяет статус оплаты

- Если всё ОК → **бронь автоматически попадает в календарь**
- Если что-то не так → клиент получает причину отказа

### 4. Ручная проверка (если нужно)

Зайдите в **"Ожидают оплаты"** в админ-панели:
- Увидите все брони с загруженными скриншотами
- Можете посмотреть результат AI-проверки
- Кнопки: **Подтвердить** или **Отклонить**

## Безопасность

✅ **Что хранится в БД:**
- Таблица `payment_links` — шаблоны ссылок для каждого объекта
- Таблица `pending_bookings` — неподтвержденные брони с URL скриншотов

✅ **Скриншоты:**
- Загружаются в S3 хранилище
- Доступны только по прямой ссылке (не индексируются)

✅ **Персональные ссылки:**
- Генерируются для каждой брони отдельно
- Содержат точную сумму и описание

## Требуемые секреты

Убедитесь, что добавлены в проект:
1. `TELEGRAM_BOT_TOKEN` — токен вашего бота
2. `OPENAI_API_KEY` — для AI-проверки скриншотов
3. `DATABASE_URL` — подключение к БД (уже есть)
4. `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` — для S3 (уже есть)

## Примеры платежных систем

### СБП (Система быстрых платежей)
```
https://qr.nspk.ru/ВАША_ССЫЛКА?sum={amount}&cur=RUB
```

### ЮMoney
```
https://yoomoney.ru/quickpay/confirm?receiver=ВАША_КАРТА&sum={amount}&label={description}
```

### Прямая карта (через форму)
```
https://example.com/pay?amount={amount}&comment={description}
```

### QIWI
```
https://qiwi.com/payment/form/99?extra['account']=НОМЕР&amountInteger={amount}
```

## Поддержка

Если возникли вопросы — проверьте:
1. Секреты добавлены?
2. Платежные ссылки настроены для всех объектов?
3. Webhook бота настроен (кнопка "Автонастройка")?
4. OPENAI_API_KEY активен?
