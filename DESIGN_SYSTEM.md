# TourConnect Premium Design System

**Философия:** Apple / Linear / Notion Calendar  
**Ключевые слова:** Премиум, Минимализм, Контроль, Спокойствие

---

## 🎨 Цветовая схема (Dark-First)

### Основные цвета

```css
--background: hsl(0 0% 7%)          /* Почти чёрный фон */
--foreground: hsl(0 0% 98%)         /* Светлый текст */
--card: hsl(0 0% 9%)                /* Карточки */
--primary: hsl(210 100% 56%)        /* Синий акцент (#1E90FF) */
```

### Семантические цвета

```css
--success: hsl(142 76% 36%)         /* Зелёный (#22c55e) */
--warning: hsl(38 92% 50%)          /* Оранжевый (#f59e0b) */
--destructive: hsl(0 72% 51%)       /* Красный (#ef4444) */
```

### Нейтральные

```css
--muted: hsl(0 0% 15%)              /* Приглушённый фон */
--muted-foreground: hsl(0 0% 60%)   /* Второстепенный текст */
--border: hsl(0 0% 18%)             /* Границы элементов */
```

### Почему эти цвета?

- **Тёмный фон (7% светлоты)** — снижает нагрузку на глаза, создаёт эффект глубины
- **Синий primary (#1E90FF)** — ассоциируется с надёжностью, технологиями, спокойствием
- **Высокий контраст текста (98% vs 7%)** — читаемость и доступность
- **Минимум цветов** — не отвлекает, фокус на контенте

---

## 📐 Типографика

### Шрифт

**Inter** — современный гротеск от Расмуса Андерссона:
- Отличная читаемость на экранах
- OpenType features: `cv02`, `cv03`, `cv04`, `cv11`
- Поддержка кириллицы
- Использован в Apple, GitHub, Stripe

### Иерархия размеров

```css
h1, h2, h3: font-weight: 700, letter-spacing: -0.02em
text-4xl (36px): Заголовки страниц
text-xl (20px): Заголовки карточек
text-base (16px): Основной текст
text-sm (14px): Описания, метки
text-xs (12px): Бейджи, вспомогательный текст
```

### Правила

- **Негативный трекинг (-0.02em)** для крупных заголовков
- **font-bold (700)** для всех заголовков
- **font-medium (500)** для кнопок
- **antialiased** — сглаживание шрифтов

---

## 🧩 Компоненты

### Button

```tsx
// Варианты
default: Синий bg-primary, для главных действий
outline: Прозрачный с рамкой, вторичные действия
ghost: Прозрачный без рамки, минимальный вид
destructive: Красный, опасные действия

// Размеры
sm: h-9, компактные кнопки (фильтры)
default: h-10, основные кнопки
lg: h-12, крупные CTA

// Эффекты
- shadow-sm → shadow-md при hover
- ring-2 ring-ring при focus
- transition-all для плавности
```

### Card

```tsx
// Базовый стиль
rounded-xl (12px радиус)
border border-border (тонкая рамка)
bg-card (тёмный фон)
shadow-lg (глубокая тень)
backdrop-blur-sm (стеклянный эффект)

// Интерактивность
hover:shadow-xl (увеличение тени)
hover:border-primary/20 (подсветка рамки)
```

### Badge

```tsx
// Варианты
default: bg-primary/10, text-primary, border-primary/20
success: bg-success/10, text-success, border-success/20
warning: bg-warning/10, text-warning, border-warning/20
destructive: bg-destructive/10, text-destructive, border-destructive/20

// Правила
- Всегда rounded-full (идеальный круг)
- px-3 py-1 (комфортный паддинг)
- text-xs font-semibold (мелкий жирный текст)
- shadow-sm (лёгкая тень для объёма)
```

### Calendar Cells (ячейки календаря)

```tsx
// Статусы бронирования
Забронировано:    bg-destructive/10 border-destructive/40
Ожидает оплаты:   bg-warning/20 border-warning/50
Ожидает подтв.:   bg-warning/10 border-warning/40 animate-pulse
Свободно:         bg-card/30 border-border

// Сегодняшний день
ring-2 ring-primary ring-offset-2 ring-offset-background

// Выходные дни (заголовки)
bg-primary/10 border-primary/30 text-primary

// Цены (динамическое ценообразование)
Высокая загрузка: bg-success/20 border-success/30 text-success
Срочное бронирование: bg-destructive/20 border-destructive/30 text-destructive
Выходные: bg-primary/20 border-primary/30 text-primary
```

---

## ✨ Визуальные эффекты

### Glassmorphism (стеклянный эффект)

```css
backdrop-blur-sm    /* Размытие фона за элементом */
bg-card/30          /* Полупрозрачный фон (30% opacity) */
border border-border /* Тонкая рамка для контраста */
```

**Где использовать:**
- Карточки объектов (UnitCard)
- Ячейки календаря
- Модальные окна
- Floating панели

### Тени

```css
shadow-sm:  Базовая тень для мелких элементов (badges)
shadow-md:  Средняя тень для кнопок в покое
shadow-lg:  Глубокая тень для карточек
shadow-xl:  Максимальная тень при hover на карточках
```

**Правило:** Тень увеличивается при hover для эффекта "подъёма"

### Переходы

```css
transition-all duration-200  /* Быстрые изменения (hover) */
transition-all duration-300  /* Медленные изменения (layout) */
animate-pulse                /* Мигание для срочных элементов */
```

**Правило:** Все интерактивные элементы имеют `transition-all`

---

## 🎯 UX Паттерны

### Иерархия действий

1. **Primary action** (синяя кнопка) — одна на экран
2. **Secondary actions** (outline кнопки) — множественные
3. **Tertiary actions** (ghost кнопки) — минимальные

### Статусы и индикаторы

- **Красный (destructive)** — забронировано, ошибки, удаление
- **Оранжевый (warning)** — требует внимания, ожидание
- **Зелёный (success)** — успех, доступно, высокая загрузка
- **Синий (primary)** — акцент, выбранное, интерактив

### Пустые состояния

```tsx
<Icon size={48} className="text-muted-foreground/50" />
<p className="text-muted-foreground">Описание что делать</p>
```

---

## 📱 Адаптивность

### Breakpoints

```css
sm:  640px   /* Мобильные */
md:  768px   /* Планшеты */
lg:  1024px  /* Десктопы */
xl:  1280px  /* Большие мониторы */
2xl: 1536px  /* Ультраширокие */
```

### Grid паттерны

```tsx
grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3
/* 1 колонка → 2 → 3 по мере роста экрана */
```

### Мобильные правила

- Минимальный размер кнопки: 44x44px (Apple HIG)
- `gap-3` минимум между кнопками
- `text-sm` или крупнее для читаемости
- Fixed позиционирование для навигации

---

## 🚫 Чего избегать

### Не делать:

❌ Пёстрые градиенты (from-purple-100 to-pink-100)  
❌ Жёсткие цвета (bg-blue-500 напрямую)  
❌ Мелкий текст (< 12px)  
❌ Низкий контраст (серый текст на сером фоне)  
❌ Более 3 уровней вложенности карточек  
❌ Анимации дольше 300ms  

### Вместо этого:

✅ Токены цветов (bg-primary, text-destructive)  
✅ Полупрозрачные слои (bg-primary/10)  
✅ Минималистичные иконки (lucide-react)  
✅ Чёткая иерархия (h1 → h2 → h3)  
✅ Одна карточка с секциями внутри  
✅ Быстрые transitions (200-300ms)  

---

## 🔧 Технические детали

### CSS переменные

Все цвета определены в `src/index.css` через HSL:

```css
--primary: 210 100% 56%
/* Использование: hsl(var(--primary)) */
```

**Преимущества HSL:**
- Легко создавать варианты с opacity: `bg-primary/10`
- Консистентность через всё приложение
- Простота переключения тем (light/dark)

### Tailwind конфиг

`tailwind.config.ts` содержит:
- Шрифты (Inter)
- Радиусы (--radius: 0.75rem)
- Анимации (fade-in, scale-in)
- Расширенные цвета (success, warning)

---

## 📊 Примеры использования

### Карточка объекта

```tsx
<div className="
  p-5 
  border-2 
  rounded-xl 
  backdrop-blur-sm
  transition-all
  bg-card/50
  border-border
  hover:border-primary/50
  hover:shadow-lg
">
  {/* Контент */}
</div>
```

### Кнопка с иконкой

```tsx
<Button size="sm" variant="outline">
  <Icon name="Plus" size={16} />
  Добавить
</Button>
```

### Статус-бейдж

```tsx
<Badge variant="success">
  {units.length} / {limits.maxUnits}
</Badge>
```

---

## 🎓 Дизайн-принципы

1. **Меньше — лучше**  
   Удаляй элементы, пока функция не пострадает

2. **Контраст — король**  
   Текст должен быть чётким, границы — видимыми

3. **Консистентность > Креативность**  
   Повторяй паттерны вместо изобретения новых

4. **Действие прежде формы**  
   Кнопки и ссылки видны сразу

5. **Воздух между элементами**  
   gap-4 минимум, gap-6 для секций

---

## 🚀 Что дальше?

### Реализовано:

✅ Цветовая схема dark-first  
✅ Шрифт Inter с OpenType  
✅ Обновлённые Button, Card, Badge  
✅ Календарь с новыми цветами  
✅ Glassmorphism эффекты  

### TODO (опционально):

- [ ] Анимации появления (fade-in, slide-up)
- [ ] Скелетон-лоадеры вместо спиннеров
- [ ] Микроинтеракции (ripple на кнопках)
- [ ] Тёмные graphviz-графики для аналитики
- [ ] Переключатель light/dark темы

---

**Версия:** 1.0  
**Дата:** 2026-01-23  
**Автор:** Юра (AI Assistant)
